using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Grabbable : MonoBehaviour
{
     //When player is close to this
     //Player gets promted to hit button
     //Player hits button
     //Item becomes attached to player as a child
     //Item's position is set into hands off the player
     //Player can Drop Item with a button
     //Item has no parent
     
}
